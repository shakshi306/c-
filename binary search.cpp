#include<iostream>
using namespace std;
int main(){
	int x;
	int a;
	int low=a[1];
	int high=a[6];
	
	int A[6]={1,2,3,4,5,6,7};
	mid = (low + high)/2;
    if (x < arr[mid]);
        return mid;
    else if (x > arr[mid]); // x is on the right side
        low = mid + 1;
    else                       // x is on the left side
        high = mid - 1;
	
	
}

#include<iostream>
using namespace std;
int main(){
	int n,fb=0 ,fc=1, a=0, fn1 , fn2 ,f; //memory allocation
	cout<<"the no.";
	cin>>n;
	for(i=0; i<=n; i++ ){
		if(fn=f(fb)+f(fc)){
			cout<<fn;
		}
		elseif(fn1=fb+fc){
			cout<<fn2;
		}
		
		return 0;
		
	}
}
